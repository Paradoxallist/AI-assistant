# Where to read archives from
TEXTS_PATH = "storage/text/"
# Where to read archives from
DB_PATH = "storage/db/db_ai_data.db"
# Which file extensions count as archives
ARCHIVE_EXTENSION = {".zip", ".tar", ".tar.gz", ".tgz", ".tar.xz", ".txz", ".tar.bz2", ".tbz2"}